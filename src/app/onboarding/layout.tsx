// 📁 /src/app/onboarding/layout.tsx
/**
 * Layout del proceso de onboarding - Solo UI
 * Estructura visual sin lógica de autenticación
 */

export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}